import React, { Component } from 'react';
import Header from '../component/header';

class Cart extends Component {
  render() {
    return (
      <div>
        <Header></Header>
      </div>
    );
  }
}

export default Cart;